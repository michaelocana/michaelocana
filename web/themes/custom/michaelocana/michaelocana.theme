<?php
use Drupal\image\Entity\ImageStyle;

/**
 * @file
 * Theme functions.
 */

function michaelocana_preprocess_page(&$variables) {
  $variables['site_name'] = \Drupal::config('system.site')->get('name');
}
